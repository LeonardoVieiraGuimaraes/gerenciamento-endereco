# Visual Studio / .NET Core
bin/
obj/
*.user
*.suo
*.userosscache
*.sln.docstates
.vscode/

# Docker
.dockerignore

# OS files
.DS_Store
Thumbs.db

# Logs
*.log

# Keycloak and DB data volumes if created locally
**/keycloak_data/
**/sqldata/

# Segredos de produção — nunca commitar, só existem no servidor, escritos pelo
# GitHub Actions ao lado do docker-compose.prod.yml (que é quem lê o .env).
.env
.env.prod
docker-compose.override.yml

# Ferramentas / ambiente local
.cloud/

# Extrações temporárias do tema do Keycloak (referência local, não versionar)
theme.zip
theme-extracted2/

# Configuração local de ferramentas de desenvolvimento
.claude/
